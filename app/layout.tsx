import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Lunr - Link Infrastructure Platform",
  description: "Complete link infrastructure: URL shortening, QR codes, campaign management, and developer APIs. Build, track, and scale your link strategy.",
  keywords: "link shortener, URL shortener, QR code generator, link analytics, URL management",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
